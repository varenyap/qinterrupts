23 June 2011

To run sqlparser.py

1) Download and install the python module psycopg to interface with postgres.
 Found at http://www.initd.org/psycopg/install/
 
2) Create a database and run the SQl commands stored in the script file.

3) Modify config.py to reflect the following:
	a) The path to the script file
	b) The Postgres user
	c) The name of the database
	d) Password to access the db

4) python sqlparser.py should run the file.
